import {StyleSheet} from 'react-native';

const SIZE = 100;
const CIRCLE_SIZE = SIZE * 3;

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  wrapper: {
    flex: 1,
    flexDirection: 'row',
  },
});
